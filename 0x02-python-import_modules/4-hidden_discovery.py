#!/usr/bin/python3
if _name _ == "_main_":
    import hidden -4
    # print sortedname from directory
    for name in sorted(dir(hidden-4)):
        print only name that donot start with -
        if name[:2] != '-':
            print("{}.format (name))
